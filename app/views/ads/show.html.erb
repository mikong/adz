<h1><%=h @ad.sponsor %> Ad</h1>

<div class="spacer"></div>

<p>
  Keywords: <b><%=h @ad.word_list %></b>
</p>

<p>
  <% if @ad.text.blank? %>
  No Custom Ad.
  <% else %>
  Custom Ad: <b><%=h @ad.text %></b>
  <% end %>
</p>

<p>
  Budget: <b><%= @ad.budget.to_currency(:ph) %></b>
</p>

<div class="spacer"></div>

<%= link_to 'Edit', url(:edit_ad, @ad) %> | <%= link_to 'Back to list', url(:ads) %>
